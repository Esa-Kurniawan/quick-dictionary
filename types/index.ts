// for wordnik api
export enum Language {
    US_ENGLISH = "en-us",
    UK_ENGLISH = "en-gb",
    FRENCH = "fn",
}

export enum Theme {
    DARK = "dark",
    LIGHT = "light",
}

export enum Endpoint {
    ENTRIES = "entries",
}
